{
  "name": "payment-verification-api",
  "version": "2.0.0",
  "description": "Payment verification API for Telegram bot (live Gmail check, no database)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "imapflow": "^1.0.161",
    "mailparser": "^3.6.6"
  },
  "devDependencies": {
    "vercel": "latest"
  },
  "engines": {
    "node": "24.x"
  }
}
